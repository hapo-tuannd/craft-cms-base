<nav class="container navbar navbar-expand-lg navbar-light bg-light">
	{% set firstSegment = craft.app.request.getSegment(1) %}
	<a class="navbar-brand" href="#">LOGO</a>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarSupportedContent">
		<ul class="navbar-nav mr-auto">
			<li class="nav-item {{ firstSegment == '' ? 'active'}}">
				<a class="nav-link" href="{{ siteUrl }}">Home
					<span class="sr-only">(current)</span>
				</a>
			</li>
			<li class="nav-item {{ firstSegment == 'blog' ? 'active'}}">
				<a class="nav-link" href="{{ url('blog') }}">Blog</a>
			</li>
			<li class="nav-item {{ firstSegment == 'about' ? 'active'}}">
				<a class="nav-link" href="{{ url('about') }}">About</a>
			</li>
		</ul>
	</div>
</nav>
